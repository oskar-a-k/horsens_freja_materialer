name: Deploy Firebase Hosting

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze project
        run: flutter analyze

      - name: Build web app
        run: flutter build web

      - name: Skip deploy notice (missing Firebase secret)
        if: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HORSENS_FREJA_MATERIALER == '' }}
        run: |
          echo "Skipping deploy: repository secret FIREBASE_SERVICE_ACCOUNT_HORSENS_FREJA_MATERIALER is not configured."

      - name: Deploy to Firebase Hosting
        if: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HORSENS_FREJA_MATERIALER != '' }}
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HORSENS_FREJA_MATERIALER }}
          channelId: live
          projectId: horsens-freja-materialer