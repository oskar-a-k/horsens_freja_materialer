name: Deploy Firebase Hosting

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  skip_if_missing_secret:
    if: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HORSENS_FREJA_MATERIALER == '' }}
    runs-on: ubuntu-latest
    steps:
      - name: Skip deploy (missing Firebase service account secret)
        run: |
          echo "Skipping deploy: repository secret FIREBASE_SERVICE_ACCOUNT_HORSENS_FREJA_MATERIALER is not configured."

  build_and_deploy:
    if: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HORSENS_FREJA_MATERIALER != '' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze project
        run: flutter analyze

      - name: Build web app
        run: flutter build web

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HORSENS_FREJA_MATERIALER }}
          channelId: live
          projectId: horsens-freja-materialer